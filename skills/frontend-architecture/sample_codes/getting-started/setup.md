# Getting Started (Vite)

1. Create a new Vite app:

```bash
npm create vite@latest my-app --template react
cd my-app
npm install
npm run dev
```

2. Add React Query / Zustand as needed:

```bash
npm install @tanstack/react-query zustand
```

3. Add `sample_codes` examples into your project to try patterns locally.
